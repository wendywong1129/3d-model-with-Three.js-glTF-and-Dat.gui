import App from "@/App";
import GLTFLoader from "@/gltf/Loader";
import "./style.css";

const app = new App();
const gltfLoader = new GLTFLoader("gltf/medieval_fantasy_book/scene.gltf");

// window.addEventListener("keydown", (e) => console.log(e));
